  https://github.com/MOGI-ROS/Week-9-10-Simple-arm/tree/main#joint-state-publishing

Todo:
  ✔ Primo scaffale con 4 libri e 2 oggetti + libri con collision (box interna con collision, di larghezza minore della mesh, lunghezza ok) @done(26-08-31) — scene:=grasp_test (default del launch), book_placer.GRASP_TEST_ENTITIES + collision_size; README "AMBIENTE (scene)"
  ✔ Implementare detach e attach joint per afferrare libri con collision @cate @done(26-09-06) — GraspManagerNode (agibot_x2_pkg_py/gripper_controller.py): /gripper/right/attach|detach unici guidati dai contatti, bridge generato (bridge_config.py), pick_test_book end-to-end; doc PickAndPlace.md (Obsidian)
  ☐ Integrare attach/detach nell'azione GRASP/PLACE di library_manager_node (publish /gripper/right/attach data='' alla chiusura)
  ☐ Unire logica riconoscimento libri e riordinamento stringhe @sara
  ☐ Script per afferrare in modo autonomo/raggiungere libri dato il punto/bbox
  ☐ Stringa per ordinamento / input utente

PIPELINE:
  - foto primo scaffale (4 libri + 2 ostacoli) fornita al robot
  - riconoscimento oggetti (bbox), distinzione tra ostacoli e libri
  - spostare ostacoli sul tavolo
  - foto con OCR ai soli libri -> riconoscimento/recupero stringhe di interesse (ISBN)
    - se non riconosciuto, rotazione del libro e nuova foto -> fatto 26-09-06: pick_test_book posa il libro copertina in giu' sotto la table_camera, library_manager rephotograph legge barcode -> ISBN -> metadati (PickAndPlace.md)
  - algoritmo di ordinamento ed estrazione libri secondo ordine richiesto (inverso)
  - reinserimento libri nell'ordine corretto nello scaffale
  - riposizionamento ostacoli sullo scaffale

RIORDINAMENTO:
  - riconoscimento libri dal dorso tramite SAM
  - quelli non identificati vengono presi in mano dal robot e con la camera (testa) rileva ISBN
      - se ISBN ok, lo rimette nella sua posizione nella libreria
      - se ISBN no, lo mette in una pila sul tavolo (da far valutare all'utente)
  - algoritmo ordinamento sulle stringhe nel json
  - libera primo scaffale e li appoggia tutti in piedi sul tavolo (copertina rigida)
  - comincia con il primo libro dal primo scompartimento del primo scaffale
  - quando il primo scaffale è pieno, libera il secondo scaffale
  - e così via, ...


NEWS:
  - in control_file.gazebo mancano i sensori di contatto per le dita destre di entrambi i gripper, possono servire o fa lo stesso?

  - aggiunti file di moveit, fare:
  sudo apt update
  sudo apt install ros-jazzy-moveit-py
  sudo apt install ros-jazzy-moveit-configs-utils
  sudo apt install ros-jazzy-moveit ros-jazzy-moveit-ros-move-group
  sudo apt install -y ros-jazzy-moveit-ros-perception

  - TODO magari fare replace di x2_hand_gazebo.urdf in agibot_x2.urdf così è più chiaro e coerente, + elimina dei warning

  - creato launch separato che avvia rviz_gaz_sim.launch per la creazione del world, e poi ci aggiunge il fattore moveit, per modularità

ESEGUIRE:
  - nel primo terminale, per avviare la simulazione con anche la roba di moveit:
  ros2 launch agibot_x2_pkg moveit_simulation.launch.py 
  - secondo terminale, il nodi di path planning in ascolto (non accade nulla ma si avvia correttamente):
  ros2 run agibot_x2_pkg_py agibot_moveit_node